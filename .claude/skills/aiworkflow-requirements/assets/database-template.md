# {テーブル/機能名} データベース仕様

> 本ドキュメントは AIWorkflowOrchestrator の仕様書です。
> 管理: .claude/skills/aiworkflow-requirements/references/

---

## 概要

{このデータベース設計が何を管理するか、主な用途を1-2文で説明}

**データベース**: Turso (libSQL)
**ORM**: Drizzle ORM

---

## テーブル定義

### {テーブル名}

**用途**: {このテーブルが管理するデータを1文で説明}

| カラム名   | 型          | NULL | デフォルト  | 説明           |
| ---------- | ----------- | ---- | ----------- | -------------- |
| id         | TEXT        | ❌   | uuid()      | 主キー（UUID） |
| name       | TEXT        | ❌   | -           | 名前           |
| status     | TEXT        | ❌   | 'PENDING'   | 状態           |
| data       | TEXT (JSON) | ✅   | -           | JSONデータ     |
| created_at | INTEGER     | ❌   | unixepoch() | 作成日時       |
| updated_at | INTEGER     | ❌   | unixepoch() | 更新日時       |

### 制約

| 制約名           | 種類     | カラム  | 説明              |
| ---------------- | -------- | ------- | ----------------- |
| pk\_{table}      | 主キー   | id      | 一意識別子        |
| uq\_{table}\_xxx | UNIQUE   | name    | 名前の一意性      |
| fk\_{table}\_xxx | 外部キー | user_id | usersテーブル参照 |

---

## インデックス設計

| インデックス名       | カラム             | 種類  | 用途           |
| -------------------- | ------------------ | ----- | -------------- |
| idx\_{table}\_status | status, created_at | BTREE | 状態別検索     |
| idx\_{table}\_user   | user_id            | BTREE | ユーザー別検索 |
| idx\_{table}\_fts    | name, description  | FTS5  | 全文検索       |

---

## リレーション

| 参照元テーブル | 参照先テーブル | 関係 | 説明                     |
| -------------- | -------------- | ---- | ------------------------ |
| {this_table}   | users          | N:1  | ユーザーが所有           |
| {this_table}   | items          | 1:N  | 複数アイテムを持つ       |
| {this_table}   | tags           | N:N  | タグ付け（中間テーブル） |

---

## マイグレーション

### 作成履歴

| バージョン | 日付       | 内容             |
| ---------- | ---------- | ---------------- |
| 0001       | YYYY-MM-DD | テーブル作成     |
| 0002       | YYYY-MM-DD | インデックス追加 |

### ロールバック手順

1. マイグレーションファイルを確認
2. down マイグレーション実行
3. データ整合性確認

---

## クエリパターン

### よく使うクエリ

| 用途     | クエリパターン                                         |
| -------- | ------------------------------------------------------ |
| 一覧取得 | SELECT \* FROM {table} WHERE status = ? LIMIT ?        |
| 検索     | SELECT \* FROM {table}\_fts WHERE {table}\_fts MATCH ? |
| 集計     | SELECT status, COUNT(\*) FROM {table} GROUP BY status  |

### パフォーマンス考慮

| 操作     | 想定頻度 | 目標応答時間 | 最適化手法       |
| -------- | -------- | ------------ | ---------------- |
| 一覧取得 | 高       | < 100ms      | インデックス活用 |
| 全文検索 | 中       | < 200ms      | FTS5使用         |
| 集計     | 低       | < 500ms      | 定期キャッシュ   |

---

## データ整合性

### ビジネスルール

| ルール | 説明                 | 検証タイミング  |
| ------ | -------------------- | --------------- |
| BR-001 | nameは空文字不可     | INSERT/UPDATE時 |
| BR-002 | 削除前に依存チェック | DELETE時        |

### トランザクション境界

| 操作 | トランザクション要否 | 理由               |
| ---- | -------------------- | ------------------ |
| 作成 | ✅                   | 関連データ同時作成 |
| 更新 | -                    | 単一テーブル       |
| 削除 | ✅                   | 依存データ削除     |

---

## 関連ドキュメント

| ドキュメント       | 説明                 |
| ------------------ | -------------------- |
| interfaces-\*.md   | 関連インターフェース |
| architecture-\*.md | 関連アーキテクチャ   |

---

## 変更履歴

| 日付       | バージョン | 変更内容 |
| ---------- | ---------- | -------- |
| YYYY-MM-DD | 1.0.0      | 初版作成 |
